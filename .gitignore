# stock-prediction-service/.gitignore

# -------------------------------------------------------------------
# Global Ignores (Applies to the entire monorepo)
# -------------------------------------------------------------------

# Operating System Files
.DS_Store
Thumbs.db
Desktop.ini
.localized

# Editor/IDE specific files
.vscode/        # VS Code settings (if you don't want to share them)
.idea/          # IntelliJ/WebStorm/PyCharm IDE files
*.sublime-project
*.sublime-workspace

# Environment variables
.env            # General .env file (if you have one at the root)
.env.local      # Local environment variables (specific to Next.js, but good to ignore globally)
.env.*.local    # Other local environment files

# Docker specific ignores
# Docker build context temporary files (though usually handled by .dockerignore within each service)
# If you have any Docker-related volumes or temporary build artifacts at the root, add them here.
# For example, if you output build logs directly to the root:
# docker-build-logs/

# -------------------------------------------------------------------
# nextjs-frontend specific ignores
# -------------------------------------------------------------------
nextjs-frontend/node_modules/
nextjs-frontend/.next/          # Next.js build output
nextjs-frontend/out/            # Next.js static export output (if used)
nextjs-frontend/npm-debug.log
nextjs-frontend/yarn-error.log
nextjs-frontend/.env.local      # Redundant if already global, but explicit is good
nextjs-frontend/.env.*.local
nextjs-frontend/build/          # Common build directory for React apps

# Next.js specific cache files
nextjs-frontend/.next/cache/

# -------------------------------------------------------------------
# python-ml-service specific ignores
# -------------------------------------------------------------------
python-ml-service/__pycache__/
python-ml-service/*.pyc
python-ml-service/*.pyo
python-ml-service/*.pyd
python-ml-service/.Python
python-ml-service/.ipynb_checkpoints/ # Jupyter Notebook checkpoints
python-ml-service/.pytest_cache/      # pytest cache
python-ml-service/.venv/              # Python virtual environment (common name)
python-ml-service/env/                # Another common name for virtual environment
python-ml-service/venv/               # Yet another common name
python-ml-service/*.log               # Log files
python-ml-service/*.sqlite3           # SQLite database files (if used)
python-ml-service/data/               # Raw data files (if not part of the repo)
python-ml-service/results/            # ML experiment results (if not part of the repo)

# ML model specific ignores (if not intended to be version controlled)
# If your models are very large and you manage them with DVC or similar,
# or if they are generated on the fly, you might want to ignore them.
# However, in your case, `stock_predictor_model.pkl` is inside `models/`
# which is copied by the Dockerfile, so you generally *want* to keep it.
# If you decide not to track them, uncomment these:
# python-ml-service/models/*.pkl
# python-ml-service/models/*.h5
# python-ml-service/models/*.joblib

# -------------------------------------------------------------------
# Docker Compose specific ignores
# -------------------------------------------------------------------
# No specific ignores needed here unless docker-compose generates files
# outside of the service directories.